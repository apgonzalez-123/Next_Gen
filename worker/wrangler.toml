name = "nextgen-votes"
main = "index.js"
compatibility_date = "2025-09-01"

# Bound to the existing `investor_profile` D1 database.
[[d1_databases]]
binding = "DB"
database_name = "investor_profile"
database_id = "9510e9ec-72b2-4dd7-bcd3-9253629a1a11"

# ADMIN_KEY guards /api/reset and /api/export. Set it as a secret, never here:
#   npx wrangler secret put ADMIN_KEY
